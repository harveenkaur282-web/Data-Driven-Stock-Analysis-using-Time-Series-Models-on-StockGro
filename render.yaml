services:
  - type: web
    name: stockgro-forecast-dashboard
    env: python
    branch: main
    buildCommand: pip install -r requirements-render.txt
    startCommand: streamlit run streamlit_app.py --server.port $PORT --server.headless true
    autoDeploy: true
